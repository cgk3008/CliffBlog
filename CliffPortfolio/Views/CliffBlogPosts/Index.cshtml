@model IEnumerable<CliffPortfolio.Models.CliffBlogPost>

@{
	ViewBag.Title = "Index";
}

<h2>Index</h2>

@if (User.IsInRole("Admin") || User.IsInRole("Moderator"))
{
	@*we will need to modify this and link it to my portfolio page*@
	<p>
		@Html.ActionLink("Create New", "Create")
	</p>

}



@*<table class="table">
	<tr>
		<th>
			@Html.DisplayNameFor(model => model.Created)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.Updated)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.Title)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.Body)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.MediaUrl)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.Published)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.Slug)
		</th>
		<th></th>
	</tr>*@

@foreach (var item in Model)
{



	@Html.DisplayFor(modelItem => item.Title)

	@Html.Raw(item.Body) @*(modelItem => item.Body)*@

	@Html.DisplayFor(modelItem => item.MediaUrl)

	@*if (Model.MediaUrl != null) //changed MediaURL to MediaUrl
		{<img class ="img-responsive" src="@Model.MediaUrl" alt=""> //changed MediaURL to MediaUrl
		}*@

	//@Html.DisplayFor(modelItem => item.Published)

	//@Html.DisplayFor(modelItem => item.Slug)

	@Html.ActionLink("Edit", "Edit", new { id = item.Id })
	@Html.ActionLink("Details", "Details", new { id = item.Id })
	@Html.ActionLink("Delete", "Delete", new { id = item.Id })

}

@*</table>*@

@section Scripts {
	@Scripts.Render("~/bundles/jqueryval")
	<script>
		// Replace the <textarea id="editor1"> with a CKEditor
		// instance, using default configuration.
		CKEDITOR.replace('editor1');
	</script>
}
